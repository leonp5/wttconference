import React from 'react';
import AppBar from '../components/AppBar';

export default {
  title: 'AppBar'
};

export function AppBarNavigation() {
  return (
    <AppBar>
      <a>Menu</a>
    </AppBar>
  );
}
